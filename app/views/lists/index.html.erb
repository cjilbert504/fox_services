<div class="center jumbotron">

    <% @list.each do |list| %>

    <div>
        <%= link_to list.name, list_path(list), style: 'color:Black; padding: 14px 25px; border: 2px solid black; border-radius: 5px;' %>
    </div>
    <hr>
    <% end %>

</div>
